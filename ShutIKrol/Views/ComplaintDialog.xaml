<Window x:Class="ShutIKrol.Views.Pages.ComplaintDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Жалоба" Height="220" Width="360"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" Text="Причина жалобы:" Margin="0,0,0,6"/>
        <TextBox Grid.Row="1" x:Name="TxtReason" TextWrapping="Wrap" AcceptsReturn="True" Margin="0,0,0,10"/>
        <Button Grid.Row="2" Content="Отправить" Click="BtnSend_Click" HorizontalAlignment="Center" Padding="15,5"/>
    </Grid>
</Window>
